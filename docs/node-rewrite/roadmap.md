# Coati Node 当前交付状态

更新：2026-09-26。当前工作区尚未提交、推送或发布。用户要求候选路由冻结，其余功能一次收尾，最后只剩真实历史数据迁移与发布交付。

## 已完成

- [x] 工具调用：搜索/抓取、原生/函数回退、授权、次数/轮数/体积限制、取消和父子记账。
- [x] 多轮用量：保留已报告缓存累计，通过coati_usage标明不完整字段，三协议JSON/SSE一致；逐轮账本保留原始值。
- [x] 推理签名：原生/可表达桥接、事件顺序、明确的preserve/text-only策略及不可表达内容拒绝；所有入口统一校验。
- [x] 六组页面：总览、全员日志、个人用量、账号/渠道、能力/缓存/搜索、令牌/设备授权。操作、权限、错误恢复按明确清单验收。
- [x] 恢复探测：独立worker、配置传递、共享认领、静默期、旧结果隔离、退出排空及启用状态日志。
- [x] 故障恢复：真实跨进程并发拒绝、强杀、过期回收、无虚构计费、回收后API恢复；不把功能通过等同生产容量。
- [x] 三协议矩阵、额度/授权、统计、数据迁移工具、容器安装/备份恢复等既有模块保留；本轮整体门禁及失败项修复后的定向复查已完成。

详细操作与分层证据见[功能收尾验收](final-functional-acceptance.md)。前端100项通过；后端整体首轮1594通过、1个补验夹具错误、2项通用调度E2E跳过；修正后相关4项通过。类型、构建、静态门禁通过，没有重复全量压测。

## 本次范围外

**候选路由冻结**：不再补旧候选架构的兼容差异，按用户要求后续重新设计。既有配置不动。

desktop、CLI、插件不在开源范围；不使用LiteLLM/pi-ai。后续Node性能优化与容量提升不混入迁移功能待办。签名不可表达时的拒绝、工具缓冲SSE、worker显式启用、UUID等明确契约见收尾记录，不能被说成Python完全无差异。

## 仅剩两项

- [ ] **真实历史数据迁移**：真实来源库导出、新Node库导入、账号/权限/凭证/登录/PAT/历史用量对账。现有本机来源缺业务数据，隔离样本不替代真实迁移。
- [ ] **发布交付**：提交/推送、远端CI、最终发布审查、版本/镜像、目标部署配置、正式切换和回退演练。生产容量与供应商账单核对属于发布环境验证。

[行为对照账本](behavior-parity.md)保留逐项差异；[历史进度](progress-history.md)保留旧快照，旧记录的pending不覆盖这里的当前交付清单。没有新付费负载，没有改Python、用户上游配置或Codex配置。
