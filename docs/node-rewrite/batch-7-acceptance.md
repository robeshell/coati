# 第七批：权限归属、部署配置与搜索闭环

2026-09-26。沿用当前未提交工作区；没有发起新的供应商调用，未修改账号、密钥或路由配置，Python参考源码不变。

## 已修复并核实

- 个人用量导出此前属于管理员请求日志权限树，改为个人用量组。0041仅按权限code修正父节点，不改角色授权，不在空库插入菜单。开发/测试库均从41迁移到42个迁移；SQL已确认实际父节点。
- 逐项核对Python网关管理API与Node的读取、新增、编辑、删除、探测、模型能力、缓存、搜索、PAT和设备确认权限。个人日志/统计沿用登录后仅本人可读；个人导出单独授权。
- 复用既有普通角色用例扩展边界：仅个人导出权限可读取/导出本人数据，但全员日志、全员统计、配额、总览和上游列表返回403；仅模型服务读取权限不能新增或探测；设备确认与PAT创建互不授予。该定向检查通过。已有跨用户所有者隔离证据仍有效，不重新全量跑一遍。
- 真实浏览器核实角色权限树中“导出个人用量”位于“我的用量”下，“请求日志”下只剩编辑配额；取消表单，未创建角色或保存授权。总览仍显示352条历史请求和0进行中，未产生新模型调用。
- Docker遗漏了代码支持的24个配置变量，现传入API与恢复worker：亲和、重试/候选窗口、健康冷却、配额预留、设备生命周期、环境后备账号、探测周期及纯搜索快捷开关。使用临时环境和`--env-file /dev/null`检查Compose解析；7个非默认覆盖值在两个服务中均一致。未启动/重启容器，未输出密钥。
- 种子和迁移链现有两文件共12项通过，包括全新临时数据库迁移和增量同步；API类型检查通过。没有增加新测试文件或重复全量压测。

## 搜索闭环继续修复

- 补齐Python的纯搜索快捷路径：仅Messages入口、单条用户消息、唯一web_search工具和固定“Perform a web search for…”引导语触发；普通问题、历史会话及其他工具仍走模型。max_uses=0不执行，AGENT_WEB_SEARCH_SHORTCUT_ENABLED可关闭。
- 成功直接返回成对的server_tool_use/result和来源文本，JSON/SSE均关联真实已结算请求ID；独立搜索返回估算账本计数，模型委托使用上游usage转换并保留缓存字段，不另造一套数字。失败继续普通闭环，鉴权/配额/取消/超时/结算失败不被吞掉。
- 修复原生搜索/抓取委托把供应商401/403/429当作调用者准入失败、提前终止候选切换的问题；客户端授权与限额拒绝仍立即停止，超时/取消不追加尝试。隔离HTTP用例确认首个账号401留账，下一账号成功；不修改用户账号或临时创建公开别名。
- 两个既有搜索相关验证文件共24项通过；新增的纯搜索回归集中在一个用例，覆盖触发边界、JSON/SSE、账本计数和失败回退。API类型及网关静态检查通过；无新的真实供应商负载。
- 源码确认多轮部分usage仍有明确差异：Python逐轮累加已出现字段，Node只输出每轮都有的汇总字段，逐次账本保留原始计数。不能把部分缓存量伪装为完整总缓存；T04继续保留未完整核实，不隐去这个差异。

## 剩余项不能混为“功能没做”

行为账本仍保留12项待完整等价核实，没有因这份分类批量改成完成：

| 分类 | 编号 | 还缺什么 |
|---|---|---|
| 并发恢复验收 | S11 | Node隔离进程故障已验证；Python/Node同条件多进程表现及生产容量尚未比较 |
| 搜索与结果契约 | T03/T04 | 原生/函数回退已有本地及部分真实证据；供应商异常全集、跨协议引用和多轮部分用量的差异仍需逐项明确 |
| 页面业务操作 | V01–V06 | 源码逐操作、Node浏览器主流程和隔离写入已有证据；普通角色完整页面流程与对应Python页面的联合验收还未完成 |
| 真实迁移与发布 | D01–D03 | 完整来源数据、迁移后真实登录/历史对账、远端CI和生产切换；隔离演练不替代这些证据 |

另外7项“有差异”是已经定位的行为，不等于7个功能缺失：

- S06：Node旧候选配置的数据结构与Python单公开路由不同，不能擅自合并用户既有配置。
- P05：跨协议不可表达的签名默认拒绝，有损text-only需显式选择；完整字段兼容仍需明确。
- A04：Node增加设备共享限流和主动拒绝；旧确认/轮询包装已修复。
- U03：UUID与Python数字记录ID不同，历史导入保留映射。
- C02：恢复探测由独立worker显式启动，默认部署方式不同。
- C03：Node增加出站和总期限防护，可接受目标/超时范围不同。
- C04：依用户决定不采用LiteLLM/pi-ai，模型目录使用显式导入。

这些条目仍保留在账本供检查，不用“全部结束”概括。后续优先完成当前环境可做的T03/T04和页面业务验收，真实来源与发布条件单独列出。
