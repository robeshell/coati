# 第九批：用量页面状态一致性

2026-09-26。局部交互修正，沿用现有组件和布局；未调用供应商、未修改账号/密钥/路由。

- 个人日志默认状态由“全部状态”改为“已结束请求”，对应API默认排除reserved的真实行为。
- 个人日志补进程中断筛选和中文显示；个人、全员日志及统计复用前端状态定义，保留管理详情对旧error/cancelled的显示兼容。统计筛选仍限定已结束状态。
- 个人导出补interrupted中文状态；进行中的显示统一为“进行中”。不改变后端筛选或计费规则。
- 个人日志刷新/翻页/重新查询时关闭旧详情并清除选择，避免保留过期记录视图。

验证：复用三个既有前端检查文件，11项通过；前端构建通过（既有chunk体积提示）。浏览器通过用户菜单进入真实/agent/my-usage入口，确认默认“已结束请求”、进程中断和进行中选项；直接加载带status=interrupted的日志链接，选中条件正确，开发库返回真实0条空态。未伪造中断记录。鼠标选择尝试未取得可靠结果，因此只把明确确认的链接恢复及页面状态计作本轮浏览器证据。

无新增测试文件。V01–V06仍不等同全角色、Python/Node全部操作联合验收，行为账本计数不变。
