# 功能逐项复核（2026-09-26）

> 当前收尾状态见[功能收尾验收](final-functional-acceptance.md)；下文按时间保存审计过程，旧pending不作为当前待办。候选路由已由用户冻结。

> 当前迁移行为状态以 [Python → Node 行为对照账本](behavior-parity.md) 为准。本文的已开发/历史阶段结论不等同于行为完全一致。

本次重新以 `portal/` 为参考核对，不能把旧 roadmap 中的勾选直接当作页面完整性或真实供应商验收证据。

## 已确认并补齐的缺口

| 范围 | 发现的问题 | 修复 |
|---|---|---|
| 运维总览 | 仅四个数字和接入引导，缺少旧看板主要功能 | 账号健康/异常/未检测/待恢复、启用路由与待处理、期间切换、趋势、模型/用户排行、权限匹配快捷入口、部分加载失败提示 |
| 全员统计 | 缺缓存命中率和缓存期限/推理/估算维度 | 补齐加权命中率、5分钟/1小时写入、推理、估算请求；按执行快照展示上游用量与缓存 |
| 个人看板 | 统计藏在日志标签内，缺少状态与近期请求 | 我的用量默认展示统计，保留日志标签；补配额状态、异常提示及近期请求 |
| 统计下钻 | 排行链接及 URL 查询未贯通 | 模型/用户排行进入对应日志筛选；统计标签和期间支持 URL；个人与全员隔离 |
| 路由健康 | 自动路由没有候选可能显示就绪，停用路由计入待处理 | 无候选告警、停用排除、公开路由列表增加就绪状态 |
| 今日进行中 | 跨午夜仍运行的请求被今日过滤遗漏 | 进行中独立统计全部未结束预留 |
| 配置交互 | 多模型选择/搜索/手动输入路径不完整 | 之前回合已修：固定候选顺序、已选标签、搜索滚动、独立手动批量添加、获取失败保留旧列表 |
| 配套一致性 | 新页面翻译和相对导入遗漏；已有组件测试缺路由上下文 | 修正翻译/别名与原用例上下文；删除用户的旧通知会话应返回401，更新过期断言 |

## 对照范围与实现入口

| Python 范围 | Node 对应实现 | 本次结论 |
|---|---|---|
| admin/dashboard 个人看板 | gateway/my-usage + UsageAnalytics | 已补状态、近期请求与下钻；只读个人 API |
| agent/ops_dashboard_page | gateway/overview | 本次补齐主要统计与运维状态 |
| usage_page / my_usage_page | gateway/requests / my-usage / UsageAnalytics | 列表、详情、配额、个人导出、筛选、排行、趋势；本次补缓存维度 |
| keys_page 模型账号 | AccountsPage、legacy-account-*、account-* | CRUD、复制、发现/手动模型、代理/头/超时、调度、健康/探测均有页面与服务实现 |
| routes_page | PublicRoutes、ResourcePage(routes)、legacy-route-*、pool-route | 公开路由、绑定/自动池、回退、旧候选迁移和回滚；本次补状态 |
| tokens_page | ResourcePage(keys)、KeyActions、legacy-pat、device | 创建/编辑/吊销/轮换、授权、期限、范围、日额/RPM/并发及用量；不是客户端实现 |
| device_confirm_page | gateway/device | 设备码确认/拒绝与服务端轮询生命周期 |
| my_channels_page | AccountsPage(personal)、personal-channel | 本人隔离、前缀、模型、发现/检查、配额限制、连接配置 |
| model_profiles_page | gateway/model-profiles、model-profile-* | 上下文/输出覆盖、候选、启停/删除、目录导入；按已确认决策不恢复 LiteLLM 自动同步 |
| cache_test_page | gateway/cache-tests、cache-test-* | 新建/逐轮记录/详情/删除/中断恢复；默认登录用户身份，可选本人访问令牌 |
| websearch_page | gateway/web-search、search-settings、server-tool-loop | 搜索配置/检查、搜索抓取执行、回退与用量记录；实网需要服务配置 |
| gateway/auth/usage API | gateway/routes + protocol/* + repository/service | 三协议矩阵、取消、配额、审计、工具与授权；现有集成检查覆盖，真实供应商另验 |
| 系统管理 | admin 用户/角色/菜单/日志/字典/任务/通知/公告/个人设置 | 基础框架已实现；本次逐页只读走查及现有隔离集成回归 |

旧 Agent API 扫描到 68 个路由定义，逐项路径映射仍见 `legacy-inventory.md`。客户端启动配置、desktop、CLI、插件和运行环境不在开源范围；状态型 Responses 存储/后台操作、LiteLLM 自动同步按已有明确边界处理，不计作本次新增任务。

## 验证与限制

- 修改统计后既有 analytics 用例通过；网关门禁 41 文件，1312 通过、36 todo。todo 仍是 unsupported-field / slow-client 矩阵验收义务，不得记成通过。
- 系统集成首轮 207 通过、1 失败、2 跳过；失败是删除用户后会话断言仍期待旧行为，现已按实际安全契约修正并针对性重跑。
- 前端首轮 81 通过、13 失败；路由上下文/新增翻译/历史相对导入修正后，受影响5文件18项通过。没有增加大量测试。
- 真实浏览器看到了开发库36条历史请求、431430 Token、缓存读取412032、加权命中95.7%；这些是此前调用记录，不是本次新压测结果。
- 浏览器只读页面走查不等于逐按钮生产验收；数据写入和隔离/RBAC由已有隔离集成测试验证。真实供应商、持久负载和发布仍单独记录。

## 本轮真实调用（补齐页面后）

- 共 35 次小量真实调用：30 次成功，3 次上游拒绝，2 次默认签名保留策略拒绝转换。日志中均已结算，无遗留运行请求。
- DeepSeek Chat、千问 Chat：各 6 次，覆盖 Chat / Responses / Messages 三入口的 JSON 与 SSE。
- 千问 Messages（使用账号已有的 qwen3.8-flash）、DeepSeek Messages、DeepSeek Responses：各 6 次通过。补测显式选择 `X-Coati-Reasoning-Policy: text-only`；因此不能把结果宣称为 Chat 无损保留推理签名。
- 原生和跨协议基本响应、SSE 错误状态、用量落库已观察；这不是工具循环、图片、长时间负载或供应商账单对账验收。
- `clinepass` 返回 403：该模型只允许 Cline 产品调用。`opencodego` 返回 400：缺少会话头；其[官方要求](https://opencode.ai/docs/go/#where-can-i-use-it)是编码 Agent 请求、真实自身 User-Agent 与稳定会话 ID。没有伪装客户端或修改用户账号配置。
- 千问 Messages 原配置的 deepseek-flash 返回 Model not exist；同账号已有 qwen3.8-flash 实测正常，未擅自删除用户模型配置。
- 搜索服务当前停用且未配置密钥，无法做外部搜索实测；已有隔离测试不等同于真实搜索供应商验收。
- 两个临时测试令牌均已撤销，十条本轮临时路由全部清理；原有 deepseek-flash 路由保留。请求记录保留用于核对统计。
- 最后增量检查：路由页面 6 项、翻译 5 项通过；API 类型检查、相关静态检查与 web 构建通过。构建仍有已有大 chunk 提示。

此处为当时的验收快照：36个矩阵占位随后分两轮关闭，最新见batch-6-acceptance.md；供应商条件和超出已运行场景的长时间压力/工具/多模态实网仍需独立证据。已实现、页面可用、隔离测试通过与真实供应商验收四种状态不混用。

### 搜索配置补齐后的追加验收

用户随后配置 Tavily；独立搜索、网页提取、Messages → DeepSeek Chat 的搜索+抓取+继续回答已真实通过，5 条账本均成功、父子关联和用量来源已核对。之前“搜索服务未配置”的阻塞已解除；具体证据与边界见 server-tools.md 的真实 Tavily 验收章节。


## 第六批页面对照增量

2026-09-26再次逐操作读旧页面后，补上模型服务的协议/健康筛选与分页、模型能力的启停筛选、公开路由启停筛选、密钥类型筛选与分页。模型能力删除末页最后一条会自动回到有效页。统计查询条件写入URL并随日志/统计标签切换保留。上述修复说明此前“页面有实现”不能直接作为完整迁移结论。

已复核的操作清单：账号新增/编辑/复制/检查/删除、模型发现与手动输入、公开路由绑定/自动池/回退/启停/删除、模型能力覆盖/启停/删除/候选/导入、缓存创建/详情/逐轮记录/删除、搜索保存/已保存配置检查/恢复环境配置、密钥创建/编辑/轮换/吊销/用量、设备确认/拒绝、个人日志筛选/详情/选择导出。写操作回归使用隔离数据库或组件mock；真实开发库只读浏览器走查。不能因此宣称所有角色和异常组合都经过双端浏览器验证。
