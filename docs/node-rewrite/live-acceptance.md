# 真实链路与持续验收（2026-09-26）

本批未提交、未发布。用户明确授权真实调用与长时测试；使用已有供应商账号和搜索服务，临时路由与临时令牌隔离，不改用户原有配置。

## 已确认

- DeepSeek Chat / Responses / Messages 三种上游 × 三种入口 × JSON/SSE：18 次真实文本调用全部成功。
- 三种入口 × JSON/SSE：6 次真实搜索流程成功；数据库有 6 条 Tavily web_search 成功子记录，各 calls=1。不能仅凭最终回答含 URL 就算工具执行通过。
- 千问 OpenAI 的 qwen3.8-flash：三种入口 × JSON/SSE 共 6 次图片识别成功；使用合成纯红 PNG，并校验返回包含 red/红。
- 临时令牌日预算 1：真实入口正确返回 HTTP 429 quota_exceeded。
- 账号选择、配额、设备、会话：复用现有隔离测试，10 项通过。实际故障切换依赖本地可控故障上游证据，本轮未人为破坏真实账号。

## 发现并修正

- 响应字段保护初版遗漏 DeepSeek Responses 的 reasoning_text 内容片段，导致跨协议 SSE 拒绝。补齐合法内容类型并验证后，18 条真实矩阵通过。
- 首版验收令牌 RPM 30 被工具内部多轮调用消耗，影响末尾两个搜索场景；改为仅临时测试令牌 RPM 120，网关限流代码未修改。
- 测试脚本对预期错误响应调用 UsageMeter 导致丢失 HTTP 判定；修复脚本后确认预算拒绝的 429。之前两次中止运行的临时资源均已清理。

## 30 分钟任务（已完成）

报告：`/tmp/coati-acceptance-1790409440378/report.json`；最终账本为同目录 `ledger.json`。进程 PID 33317。临时 key IDs 为 14/15/16，route IDs 以报告为准。

并发上限 2，每批约 20 秒，固定长前缀，三种入口和 JSON/SSE 轮换，绑定 DeepSeek Chat 上游。每次成功请求核对响应与 gw_requests 的输入、输出、缓存读取量；每批记录 RSS 与未结算数量。连续三批失败、用量不一致或遗留预留会中止。结束撤销令牌并删除临时路由，保留账本用于对账。

持续窗口实测 30.004 分钟，90 批、180 次调用全部成功，错误率 0%。输入 438,300 Token，输出 5,306 Token，缓存读取 409,578 Token，占输入 93.45%。P50 809 ms、P95 1,143 ms，计时包含完整响应读取。

90 个 RSS 样本从 155.92 MiB 到 115.47 MiB，范围 102.33–155.92 MiB；本窗口没有观察到持续增长，不能据此排除更长运行中的泄漏。每批未结算请求为 0。最终重新核对 180 条线上用量和账本：输入、输出、缓存读取及成功状态全部一致。

整轮账本 223 条：222 条 ok、1 条预期 quota_exceeded（含内部模型/搜索子调用，不能当作 223 次独立外部请求）。6 条 Tavily 搜索子记录均成功、calls=1，父请求 ID 均对应 6 条工具验收请求。

已直接查询数据库确认临时令牌 14/15/16 全部 revoked，临时路由 21/22/23/24 全部删除，无 reserved 请求；进程已退出。跟进自动化已暂停，不会再开新的付费负载。

## 边界

真实基础、图片、搜索验证覆盖上述具体账号与模型；不等同于供应商全功能兼容。后续第六批已将18项广义unsupported-field占位替换为具体本地wire检查；它们不属于本报告的真实供应商结果，任意嵌套扩展/包络元数据仍不在完整性承诺内。30 分钟低并发测试也不能证明生产容量、多进程故障恢复或 Node 相对 Python 的性能提升；费用金额需供应商账单另行核对。
