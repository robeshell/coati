# ============================================================
# coati 门户前端（portal/frontend）环境变量示例
# 复制为 .env.development（或 .env.production）后按本机情况填写
# .env.* 已在 .gitignore 忽略，本模板文件放行；勿提交本机地址
# ============================================================

# Vite dev server 的代理目标。/api、/ws、/site 三条转发都只读这一个键
# （见 vite.config.js 的 server.proxy）；/ws 的地址由它把 http(s) 换成 ws(s) 得到，不单独配。
#
# 不设置时回落到 http://localhost:5001，也就是 `flask run --port=5001` 的默认端口。
# 只有换端口、跑第二个后端实例、或后端在别的机器上时才需要改。
#   常规开发：          http://127.0.0.1:5001
#   本机另起实例：      http://127.0.0.1:5003
#   不要指向 Docker 部署（那是宿主 8080 → 容器 5000，dev 前端直连线上后端会绕过本地代码）
VITE_API_PROXY_TARGET=http://127.0.0.1:5001
